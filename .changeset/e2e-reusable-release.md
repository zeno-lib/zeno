---
"@zeno-lib/e2e": patch
---

First usable release. Ship a shared Playwright `baseConfig` preset via `@zeno-lib/e2e/config` and a parameterized app-dependency verifier via `@zeno-lib/e2e/verify-deps` (plus the `zeno-verify-app-deps` bin).
